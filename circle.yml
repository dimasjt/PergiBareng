machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  ruby:
    version: 2.3.1
  node:
    version: 8.1.0

dependencies:
  override:
    - bundle install
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - bundle exec rspec
