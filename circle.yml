machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"
  ruby:
    version: 2.3.1

dependencies:
  override:
    - bundle install
    - yarn
  cache_directories:
    - ~/.cache/yarn

test:
  override:
    - bundle exec rspec
    - yarn test
